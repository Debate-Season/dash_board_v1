import 'package:flutter/material.dart';

class AuthControllers extends StatelessWidget {
  const AuthControllers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
