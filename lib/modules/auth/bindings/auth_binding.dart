import 'package:flutter/material.dart';

class AuthBinding extends StatelessWidget {
  const AuthBinding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
